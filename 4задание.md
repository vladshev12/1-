1.реализация

```python
    def table(n):
    a = [[0 for i in range(n)] for i in range(n)]
    for i in range(0,n):
        for j in range(0,n):
            a[i][j]=(i+1)*(j+1)
    return a
    
```

2.измерение времени
```python
    def measure_time(func, data):
    start = time.perf_counter()
    func(data)
    end = time.perf_counter()
    return end - start
```
3.Эксперименты
```python
    if __name__ == '__main__':
    sizes = [100, 1000, 5000, 10000]
    for n in sizes:
        t = measure_time(table, n)
        print(n, t*1000, "ms",table(n))
```
|         n          |            t           |
|--------------------|------------------------|
| 100                | 0.000291 s             |
| 1000               | 0.0397 s               |
| 5000               | 1.113 s                |
| 10000              | 4.887 s.               |
